import SwiftUI

/// 설정. (스텁 — Task 8 에서 앱 정보·데이터 등 추가)
struct SettingsView: View {
  var body: some View {
    NavigationStack {
      List {
        Text("HomePin")
      }
      .navigationTitle("설정")
    }
  }
}
