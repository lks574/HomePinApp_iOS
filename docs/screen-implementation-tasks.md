---
aliases: [screen-tasks, 화면 구현 태스크]
tags: [doc/code, screens, tasks]
created: 2026-06-12
updated: 2026-06-18
status: draft
---

# 화면 구현 태스크

> 최근 추가: `screen-19` CloudKit 동기화 + 가족공유 횡단 — Settings iCloud Sync 진입점·SwiftData CloudKit private DB 골격·CloudKit 호환 스키마 선행 정리·가족공유 CKShare 스냅샷 초대/가져오기.
> 이전 추가: `screen-18` AdMob 광고 수익화 횡단 — 첫 외부 의존성(GoogleMobileAds SDK, iOS 전용 링크)·UMP 동의·ATT·전면(장보기 완료)·보상형 opt-in(개발자 응원), macOS no-op(화면 추가 아님, 수익화 기능).
> 이전 추가: `screen-17` macOS 플랫폼 지원 횡단 — 네이티브 macOS 타깃 추가·소스 공유·`#if os` 흡수·플랫폼 추상화·스킴 분리(화면 추가 아님, 플랫폼 확장).

화면별 구현 진도(단일 진실 소스)를 `screen-XX` ID로 관리한다.

| ID | 화면 | 상태 | 메모 |
| --- | --- | --- | --- |
| screen-01 | 시안 C 5탭 셸·홈·장소·레시피·추가 시트 | done | UI 우선 1차. 홈은 장보기 요약·장소 바로가기·최근 추가·임박 배너가 실제 이동/편집으로 연결됨. 검색/AI/STT는 후속으로 진행. |
| screen-02 | ItemEditor | in-progress | 물건 추가/편집 공용 에디터. 이름·수량·장소·세부위치·분류·태그·유통기한·메모 지원. 물건 사진 선택/저장은 제거. 분류/태그는 기존 선택 + 신규 생성 가능. 편집 모드 삭제 지원, 수량 1에서 `-` 탭 시 삭제 확인으로 이어짐. 장소 상세 물건 행 context menu 에 "다 썼어요" 빠른 정리 추가. |
| screen-05 | PlaceEditor (장소 CRUD) | in-progress | 장소(Area) 추가/편집 공용 에디터 + 목록·상세에서 삭제(확인). 삭제 시 Spot cascade, 물건은 nullify 보존. |
| screen-06 | SpotEditor (세부위치 CRUD) | in-progress | 세부위치(Spot) 추가/편집 공용 에디터 + 상세에서 삭제(확인). 추가·편집은 상세 ⋯/카드 메뉴, 삭제 시 물건은 nullify 로 ‘수납공간 미지정’ 이동. |
| screen-03 | RecipeDetail (레시피 상세) | in-progress | 레시피 카드 → 상세 push. 헤더(메타·요약)·재고 요약·재료 목록(보유/임박/없음 칩)·조리 단계. 상단 ⋯ 로 편집/삭제(확인). |
| screen-07 | RecipeEditor (레시피 CRUD) | in-progress | 레시피 추가/편집 공용 시트 + 상세에서 삭제. 기본정보(제목·종류·인분·시간·요약) + 재료/단계 동적 행 편집. 저장 시 재료 이름→보유 물건 매칭으로 재고 상태 연동. 레시피 목록은 검색어 + cuisine + dishType AND 필터 적용. |
| screen-08 | Settings (설정 고도화) | in-progress | 표시(테마 시스템/라이트/다크, `@AppStorage`+`preferredColorScheme` · 언어 시스템/English/한국어, `AppLanguagePreference` `@AppStorage`+루트 `.environment(\.locale)` 즉시 전환) · 데이터(저장 현황 + 전체 데이터 정리, 기본 Space 복구) · 정보. 다크는 디자인 토큰 라이트/다크 동적화로 앱 전체 적용. |
| screen-04 | 검색 (중앙 시트) | in-progress | **검색-우선 통합** UX — [추가\|검색] 모드 토글 제거, 단일 입력 필드 하나(타이핑·음성 공용). 입력 즉시 기존 항목 실시간 검색: 물건(이름·위치 경로·장소·세부위치·분류·태그·메모·수량 부분 일치) + 레시피(제목·요약·cuisine/dishType 라벨·태그·재료명/단위/메모 부분 일치)를 물건/레시피 섹션으로 구분 표시. 자연어 문장은 로컬 규칙(`CaptureSearchQuery`)으로 조사·불용어를 제거한 토큰 전체 일치 + 속성 플래그(임박/만료/위치 없음/지금 만들 수 있음/부족 재료)로 보강. 결과 아래 **항상 `+ "{입력어}" 추가하기` 행**을 두고, 이 행을 명시적으로 눌러야만 추가 진행(AI 가 추가/검색 의도 자동추측 안 함 — 오분류 데이터 오염 방지). 물건 추가는 장보기 미등록 항목 재고 생성과 같은 `ItemEditor` `create(initialName:)` 화면으로 통일. 음성(`SpeechDictation` 온디바이스 받아쓰기)은 단일 입력 필드를 채우고 `추가하기` 누르면 같은 에디터 경로로 합류, 권한·불가용·거부 폴백. 물건 결과 탭 시 `ItemEditor` 편집, 레시피 결과 탭 시 시트 닫고 레시피 탭 상세 push(`AppRouter.openRecipe`, `recipesPath` 바인딩). 중앙 버튼 아이콘은 `sparkles`(AI). 홈/장소의 정적 검색바는 제거해 죽은 입력을 없앴다. (find/add 의도 자동판별은 본 통합으로 대체·불필요.) **검색 랭킹·초성·편집거리 fallback·빈 입력 추천 칩·중복 추가 가드는 `screen-16`(검색·추가 정교화 횡단)에서 추가.** |
| screen-09 | 확인 드래프트 (AI 추가) | in-progress | AI 파서 결과를 확인/수정/삭제하는 push 화면(`CaptureSheet` 소유). 항목별 이름·수량·장소·세부위치·분류·태그, 신규 vs 기존 매칭 시각 구분(‘신규’ 칩), 다건 배열, 장소 비면 채워야 저장. **분류·태그 인라인 편집**: 값이 비어도 분류·태그 행을 항상 노출, 분류 칩 탭 → 드래프트 전용 시트(`DraftCategoryPickerSheet`)에서 후보 선택·자유입력·없음, 태그 칩별 제거(x)·“+ Tag” 추가 칩 → `DraftTagPickerSheet`. 시트는 insert 없이 `NameMatch`(`AddDraftResolver.match`/`newMatch` 정규화 판정)만 갱신해 신규/기존 칩 시각 규약 자동 유지. 저장 시 `AddDraftResolver` 가 다건 일괄 insert + 신규 위치/분류/태그 생성(없으면생성/있으면매핑, `Item.normalize` 매칭) 후 시트 dismiss. |
| screen-10 | 다국어(i18n) 횡단 | done | 영어/한국어 + 시스템 추종 기본(기본 en) + 설정 수동 오버라이드(시스템/English/한국어, `AppLanguagePreference` `@AppStorage`, 루트 `.environment(\.locale)` 즉시 전환·재시작 불필요). UI 텍스트 단일 String Catalog(`Localizable.xcstrings`), 권한 문구 `InfoPlist.xcstrings`, `Project.swift` developmentRegion en + CFBundleLocalizations en/ko. 사용자입력·로그·NL프롬프트 비대상. cuisine/dishType 저장값(raw 한국어) 유지·표시 라벨만 매핑(`RecipeClassification`). 시드는 DEBUG 게이트 유지·시스템 언어로 텍스트 세트 분기(`SeedText`, 코드 내 en/ko). 설정에서 언어 수동 선택 추가(`AppLanguagePreference`, 시스템/English/한국어). 결정: `docs/wiki/Decision/2026-06-16-i18n-다국어화-방침.md`. 잔여: STT locale 시스템 추종·영어 NL 추출 품질(follow-up). |
| screen-11 | Shopping (장보기) | done | 신규 `ShoppingItem` @Model + 홈 장보기 요약 섹션(미완료 N개 + 상위 3개 미리보기 → push) + `ShoppingListView` 풀 화면 CRUD(수동 추가·체크 토글·삭제, 미완료/완료 섹션 분리). `RecipeDetail` 부족 주재료에서 `ShoppingItem(sourceIngredient:)` 자동 추가(부재료 제외, 같은 재료/같은 이름 미완료 항목 중복 방지, 완료된 원본 항목은 미완료로 복구). 미완료 항목 체크 시 같은 이름의 기존 `Item` 이 있으면 수량 증가, 없으면 `ItemEditor` 로 위치 선택 후 신규 재고 생성. `sourceIngredient` 가 있으면 반영된 `Item` 을 연결해 레시피 부족 상태도 해소. 체크 해제는 장보기 상태만 되돌리고 재고는 자동 차감하지 않음. View↔SwiftData 직결(`@Query`+`modelContext`). 결정: `docs/wiki/Decision/2026-06-16-장보기-데이터모델.md`. |
| screen-12 | 레시피 자연어 추가 | in-progress | 중앙 ✨ 시트(`CaptureSheet`)에 **명시적 "Add a recipe" 행**을 항상 노출(검색-우선 통합 UX 유지, 의도 자동추측 없음) → 레시피 전용 입력 화면 `RecipeCaptureView` 시트. 여러 줄 텍스트 = **타이핑+붙여넣기+음성**(`SpeechDictationViewModel` 재사용, 단일 경로). 사진/카메라 OCR 진입점은 제거. "Sort with AI" 명시적 탭 → 온디바이스 Foundation Models `@Generable ParsedRecipe`(제목·cuisine·dishType·servings·totalMinutes·재료배열`[name,quantity]`·단계 텍스트배열; **단계 타이머 파싱 제외**) 추출(`NLRecipeParser`, 비-MainActor) → `NLRecipeParseViewModel`(@MainActor, 가용성 게이트·단일 세션 Task) → `RecipeDraftResolver`(분류 raw 정규화·draft 변환) → **확인 화면은 기존 `RecipeEditorView` 를 `RecipeEditorModel(prefill:)` 로 재사용**(AI prefill 배너 + 빈 행 추가로 교정 친화). 저장은 수동 create 와 동일(재료 이름→보유 `Item` 정규화 매칭으로 grounding). 미가용·실패·취소·빈 결과면 수동 `RecipeEditorView` 폴백. `@Model` 스키마 변경 없음(신규는 비영속 `ParsedRecipe`·`RecipeEditorPrefill` 뿐). 부족분→장보기 자동 생성은 screen-11 에서 완료. 결정: `docs/wiki/Decision/2026-06-16-레시피-NL파서-ParsedRecipe.md`. 잔여: 실기기 한국어 추출 품질. |
| screen-14 | 레시피 부재료(optional) 횡단 | in-progress | 주재료와 별도로 **부재료(곁들임·취향껏·선택적)** 입력·표시. `RecipeIngredient.isOptional: Bool = false` 가산 필드(마이그레이션 불필요, 기본 false=주재료). `Recipe`의 `missingIngredients`·`isReadyToCook`·`inStockCount`·신규 `mainIngredients`/`mainIngredientCount`·`usesExpiringIngredient` 가 **주재료(`!isOptional`)만** 집계 → "지금 가능" 판정·부족분→장보기에서 부재료 제외(단 부재료도 `item` grounding·`stockStatus` 보유 표시는 동일). `RecipeEditorView` 재료 입력을 **주재료 섹션/부재료 섹션**으로 분리(부재료는 비어도 됨, 빈 안내), `RecipeEditorModel` 은 `mainIngredients`/`optionalIngredients` 두 draft 배열·섹션별 add/remove·연속 sortOrder 부여. `RecipeDetailView` 는 주재료/부재료 섹션 구분 표시(부재료 "선택" 라벨)·요약 분모 주재료 기준. AI 파서 `ParsedIngredient.isOptional`+`@Guide`·instructions 주/부 구분 안내, `RecipeDraftResolver`→`RecipeEditorPrefill.Ingredient.isOptional` 전달, 확인 화면 교정 친화. 신규 UI 문자열 `Localizable.xcstrings` en/ko. `@Model` 스키마는 가산 필드만(기존 데이터/시드 주재료로 자연 동작). 횡단 화면: screen-03·screen-07·screen-12. |
| screen-15 | 데이터 백업/가져오기 + CSV 대량 입력 | in-progress | `Settings > Data > Backup & Import` 진입(`DataTransferView` push). **전체 백업** = 디렉터리 패키지(`.homepinbackup`, exported UTType `com.sro.homepinappios.backup`, `com.apple.package` 준수) — `data.json`(schemaVersion 메타 + 9 엔티티 DTO). 외부 의존성 없이 `FileManager` 만(ZIP 불채택, `Project.swift` `dependencies: []` 유지). export = 전체 fetch→DTO→`fileExporter`(`BackupDocument` 디렉터리 `FileWrapper`). import = `fileImporter`→패키지 읽기→디코드→schemaVersion 가드(상위 거부)→**2-pass upsert**(`BackupUpsertEngine`: pass-1 id 로 fetch→있으면 필드 갱신/없으면 insert, pass-2 UUID 참조 관계 재연결). 엔티티 단위 실패 스킵 + 결과 요약. DTO 관계는 전부 **UUID 참조**(`RecipeStep` 만 Recipe DTO 안 값배열 인라인). import 정책 = **id(UUID) upsert/덮어쓰기**. **CSV 대량 입력** = Item·Recipe 두 대상, 3파일(`items.csv`·`recipes.csv`·`recipe_ingredients.csv`, 재료는 `recipeTitle` 로 연결) — RFC4180 최소 자체 파서/직렬화(`CSV`, 따옴표·콤마·개행 이스케이프), 템플릿 export 포함. upsert 키 = `id` 있으면 id, 없으면 `normalizedName`(Item)/`title`(Recipe). area/spot/category/tags 는 이름 조회→없으면 생성(중복 방지 캐시). 행 단위 실패 스킵 + 결과 요약. 엔진 강제: `name` 설정 시 `normalizedName` 동반 갱신(`Item.normalize`), 불변식 `spot != nil → item.area == spot.area`, `ShoppingItem.stockCredited` 값 보존. 얇은 `@Observable DataTransferModel`(비영속 UI 상태 + 다단계 쓰기 오케스트레이션 두 조건 충족). 함께 `SettingsView.clearAllData()` 의 `ShoppingItem` 삭제 누락 수정(export 9종과 일치). 결정: `docs/wiki/Decision/2026-06-17-데이터-백업-번들포맷.md`, `docs/wiki/Decision/2026-06-17-CSV-대량입력-스키마.md`. 잔여: `VersionedSchema` 연동(follow-up). |
| screen-16 | 검색·추가 정교화 횡단 | in-progress | 중앙 검색([[Capture]], `CaptureSheet`)·물건 추가 경로의 횡단 정교화(새 화면 아님, `Shared/Search/`). **(1) 자연어 규칙 외부화** — 조사 접미사·불용어·의미 단어 3개 컬렉션을 `Resources/SearchRules.json` + `SearchRules.swift`(번들 1회 로드 캐시, 누락 시 `preconditionFailure`)로 분리. 토큰화 순서·`Item.normalize` 호출 순서 동일(회귀 0). **(2) 랭킹 정렬** — `SearchRanking`·`MatchTier`(exact/prefix/contains/choseong/fuzzy)·`SearchRank`. 매칭 함수가 Bool 대신 `SearchRank?` 반환 → `compactMap`+`sorted`(등급 → 상태가중치(물건 임박↑·위치없음↓, 레시피 지금가능·임박재료↑) → 이름순). **(3) 초성 검색** — `Hangul`(완성형 음절 0xAC00~0xD7A3→초성, 유니코드 산술). 질의가 전부 호환 자모 초성(0x3131~0x314E)일 때만 활성, 일반 텍스트 질의 회귀 0, `.choseong` 등급. **(4) 편집거리 fallback** — `Levenshtein`(음절 단위 2행 DP). 정상 매칭 결과 0 + `hasConcreteSignal`=true 일 때만 토큰별 편집거리 임계(길이≤3→1, 그 외 2) 이내 근사 매칭, `.fuzzy` 등급. **(5) 추천 칩** — 빈 입력 시 최근 검색어(`@AppStorage("recentSearches")` JSON, 최대 8·최신우선·정규화 중복제거, `RecentSearches`) + 임박 물건 상위. 칩 탭 = 입력 채우기만(자동 검색/추가 없음), 기록 적재는 결과 탭·추가 진입 시점. **(6) 중복 추가 가드** — 추가 진입 시 같은 `normalizedName` 기존 물건 있으면 "수량 합치기/새로 추가/취소" `confirmationDialog` **제안**(자동 합치기·저장 없음). 합치기→기존 물건 `ItemEditor` `edit`(수량 가산은 사용자 저장), 새로 추가→기존 파서/스텁 경로. `normalizedName` 동등만(부분일치 아님). **(7) 직전 위치 prefill** — `@AppStorage("lastAreaID"/"lastSpotID")`(UUID String), 저장 성공 시 기록. `ItemEditorView` create 진입 시 위치 미지정이면 last id 로 fetch 해 prefill(`selectedArea = spot?.area ?? area`, 삭제된 위치는 fetch nil→무시). 모든 경로가 `hasConcreteSignal` 가드·`normalizedName` 단일 매칭키·위치 불변식을 우회하지 않음. 새 `@Model`·외부 의존성 없음. 결정: `docs/wiki/Decision/2026-06-17-자연어-규칙-외부화.md`, `docs/wiki/Decision/2026-06-17-검색-랭킹-초성-편집거리.md`. 잔여: 편집거리 자모 정밀화·의미플래그 후보 외부화(follow-up). |
| screen-13 | 레시피 OCR 입력 (사진·카메라) | removed | 사진/카메라 OCR 입력 기능은 제거. `RecipeCaptureView` 는 텍스트·붙여넣기·음성 입력만 지원한다. |

| screen-17 | macOS 플랫폼 지원 횡단 | in-progress | 네이티브 macOS 앱 타깃 추가(화면 추가 아님, 플랫폼 확장). `Project.swift` 에 `HomePinApp-macOS`(`.macOS("26.0")`, bundleId `com.sro.homepinappmac`, entitlements `Tuist/Support/HomePinApp-macOS.entitlements`: app-sandbox·files.user-selected.read-write·device.audio-input) 추가. **소스·리소스 한 벌 공유**(둘 다 `HomePinApp/Sources|Resources/**`), 플랫폼 차이는 코드 `#if os(macOS)`/`#if os(iOS)` 로 흡수. **Mac Catalyst·Designed-for-iPad 아닌 네이티브** macOS. 플랫폼 nav/입력 modifier 헬퍼 `compactNavTitle`/`hideNavBar`/`plainTextInput`/`numericKeyboard` macOS no-op + 동적 색 provider UIColor/NSColor 분기(`Color+Hex`). STT(`SpeechAnalyzer`, macOS 는 `AVAudioSession` 분기만 skip)·FoundationModels NL 파서는 양 플랫폼 공통. 마이크·음성인식 usage description 공유(`sharedInfoPlist`). 셸(`RootTabView`)·네비게이션 재설계 없이 그대로 동작(macOS UX 최적화는 범위 밖). **스킴 분리**(`targetSchemesGrouping: .notGrouped`) → `HomePinApp`(iOS)/`HomePinApp-macOS`(macOS), 한 스킴 묶음은 macOS 빌드 시 iOS 타깃까지 끌려와 서명 오류라 분리. 동작 로직 변경 없음(iOS 회귀 0). 결정: `docs/wiki/Decision/2026-06-17-macOS-네이티브-타깃-추가.md`. 잔여: 실기 런타임 검증(AC-005~006: 파일 Import/Export·STT 권한/받아쓰기), macOS UX 최적화. |
| screen-18 | AdMob 광고 수익화 횡단 | in-progress | iOS 광고 수익화(화면 추가 아님, 수익화 기능). **첫 외부 의존성** — `Project.swift` 에 GoogleMobileAds SDK(SPM `13.5.0` `.exact`)+UMP(`3.1.0` `.exact`), **iOS 타깃에만 링크**(macOS `dependencies: []` 유지 → SDK 미링크·빌드 green). 광고 코드는 `#if os(iOS)`+`canImport(GoogleMobileAds)` 가드, macOS no-op. **포맷 = 전면+보상형만**(배너·네이티브·앱오픈 제외). **P1** SDK·동의·ATT 골격 — `AdService`(`@MainActor @Observable`, `Features/Monetization/`), 시작 플로우 `AppModel.start(adService:)`→`AdService.startup()`: UMP 동의→ATT→`MobileAds.start()`(거부해도 비개인화 진행·차단 없음), iOS Info.plist `GADApplicationIdentifier`(테스트 앱ID)·`SKAdNetworkItems`·`NSUserTrackingUsageDescription`(en/ko), 앱 레벨 `PrivacyInfo.xcprivacy`(`Resources-iOS/`). **P2** 전면 — 테스트 단위ID, 장보기 세션 완료 암묵 트리거(`ShoppingListView.onDisappear` + 이번 세션 신규 완료 ≥1, onAppear 리셋), 하루1회+최소간격 캡(`UserDefaults`), R6 정책(앱 시작·탭 전환 표시 0), best-effort(미로드·실패·캡 미통과 비차단). **P3** 보상형 opt-in — 테스트 단위ID, Settings "개발자 응원하기" 행에서만 호출(자동/강제 노출 0), 보상 = 상징적 응원 카운터(`UserDefaults`, **기능 잠금 0**), `userDidEarnReward` 수신 시에만 적립. delegate(nonisolated 콜백)는 NSObject 분리+`Task { @MainActor in }` hop(격리 트랩 회피), Swift6 경고 0. iOS·macOS 빌드 둘 다 green. 리뷰 APPROVE(R1–R11 충족). 결정: `docs/wiki/Decision/2026-06-17-AdMob-광고-수익화-도입.md`. 잔여: 출시 전 실광고 앱/단위ID 교체, ATT/동의·전면/보상형 실기 런타임 검증, (관찰)전면 최소간격 의미 명료화·보상형 로드 실패 자가 회복. |
| screen-19 | CloudKit 동기화 + 가족공유 횡단 | in-progress | Settings > Data 에 `iCloud Sync` 토글과 iCloud 계정 상태 확인 추가. 토글을 켤 때 `CKContainer.accountStatus()` 가 `.available` 이어야 켜고, 불가하면 alert 로 사유 표시. SwiftData `AppModelContainer` 가 `cloudSync.isEnabled` 값에 따라 `.none` 또는 `.private("iCloud.com.sro.homepinapp")` 로 구성된다(토글 변경은 다음 앱 시작부터 적용). CloudKit store 생성 실패 시 `cloudSync.isEnabled` 를 끄고 fallback 사유를 저장한 뒤 로컬 `.none` store 로 시작하며 Settings 에 사유를 표시한다. CloudKit 호환 선행 정리: 전 `@Model` `id` `.unique` 제거, to-many 관계 optional화. iOS/macOS CloudKit entitlements 추가, iOS `UIBackgroundModes=remote-notification`, Info.plist `CKSharingSupported=true`. 가족공유 1차: iOS `Share Home Data` 버튼 → custom zone `HomePinFamilyHome` root record 에 현재 `BackupBundle` JSON 스냅샷을 저장 → `CKShare` + `UICloudSharingController` 초대 UI 표시. 2차: 초대 수락은 app delegate 에서 `CKContainer.accept(_:)` 후 `hierarchicalRootRecordID` 저장, `Import Shared Home Data` 버튼은 `sharedCloudDatabase` root snapshot 을 fetch/decode 해 `BackupUpsertEngine` 으로 로컬 병합. macOS iCloud entitlement 는 실행 가능한 서명 빌드에 Apple Development 인증서 필요 → 서명 없는 컴파일 검증은 `CODE_SIGNING_ALLOWED=NO`. 결정: `docs/wiki/Decision/2026-06-17-CloudKit-private-동기화-골격.md`, `docs/wiki/Decision/2026-06-18-CloudKit-가족공유-1차-스냅샷.md`, `docs/wiki/Decision/2026-06-18-CloudKit-가족공유-2차-가져오기.md`. **2026-06-18: App ID 에 iCloud capability·컨테이너 미등록 → 실기기 서명 실패로 iOS/macOS entitlements 의 CloudKit 키 임시 제거(주석), 빌드 green. 계정 등록 후 복구 필요.** 잔여: Apple Developer 포털에서 container 생성/확인 + entitlement 복구, 실기기/실계정 private sync·초대·가져오기 검증, 참가자 push, 충돌 처리. |

## 후속 후보

- `screen-04` 검색 결과(잔여) — 자연어 검색 확장. 홈/장소 정적 검색바는 제거. (레시피 NL 추가는 screen-12 로 분리·착수. find/add 의도 자동판별은 검색-우선 통합으로 대체.)
- 장소/세부위치 에디터 확장 — 아이콘·Space(공간) 선택. (현재는 이름만, Space 는 첫 공간 자동 배정)
